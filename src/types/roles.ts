/** Contains all the options for the roles for different creeps */
export const enum MyCreepRoles {
    harvester,
    upgrader,
    builder,
    miner,
    claimer,
    remoteBuilder,
    escapeCase
}
